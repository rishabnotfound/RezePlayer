/**
 * This file is kept for backwards compatibility
 * All server configuration is now done via __REZEPLAYER_CONFIG__ in rezeplayer.tsx
 * No hardcoded servers needed - everything comes from user's RezePlayer.make() options
 */
